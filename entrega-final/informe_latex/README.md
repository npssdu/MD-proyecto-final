# Informe LaTeX

Este directorio contiene el informe final en LaTeX.

Archivo principal:

- `main.tex`

Para compilarlo en un entorno con LaTeX instalado:

```bash
pdflatex main.tex
pdflatex main.tex
```

El documento sigue los `Lineamientos_entrega_final.md` y resume el flujo implementado en `MD_Proyecto_Final_Completo.ipynb`.
